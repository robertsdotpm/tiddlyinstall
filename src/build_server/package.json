{
  "name": "tiddlyinstall-backend",
  "version": "0.1.0",
  "private": true,
  "description": "TiddlyInstall build server (Node.js): the HTTP API, the BullMQ workers and the site, on the src/shared/ core",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "bullmq": "^6.3.7",
    "ioredis": "^6.0.0"
  }
}
